Goal:
Apply the reviewer persona rules to the proposed implementation packet.

Review focus:
- treat task scope, declared files, constraints, and done conditions as the authority
- verify only against evidence in the packet and the reviewer persona rules
- do not rewrite the task, expand scope, or propose redesigns outside the declared objective

Output:
- return JSON only
- use this schema exactly:
{
  "decision": "approve|changes_requested",
  "reviewer": "<persona reviewer name>",
  "violations": [{"file": "...", "rule": "...", "evidence": "..."}],
  "required_fixes": ["..."],
  "evidence": [{"file": "...", "reason": "..."}]
}
