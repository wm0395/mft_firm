# Research Projects

Store one directory per research project. Keep the contents deterministic and
small.

Typical contents:

- `README.md`
- `project.json`
- `parameter-grid.json`

Use the `template/` directory as the starting point for new work.
