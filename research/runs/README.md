# Research Runs

Keep one run directory per executed parameter search or evaluation pass.

Each run should record:

- the project identifier
- the dataset snapshot identifier
- the CLI command used
- the output envelope

Use `template/` for a minimal, deterministic record.
